{
  "name": "lit-template",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "npm run build -- --watch",
    "serve": "web-dev-server --node-resolve --watch --app-index src/ --root-dir src/ --open",
    "dev": "npm run build:watch & npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.31",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "lit": "^2.2.3",
    "rxjs": "^7.5.5"
  }
}